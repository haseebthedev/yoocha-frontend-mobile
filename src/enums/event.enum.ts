export enum EventEnum {
  SEND_FRIEND_REQUEST = "send_friend_request",
  JOIN_ROOM = "join_room",
  SEND_MESSAGE = "send_message",
}

export enum EventEnumRole {
  INITIATOR = "INITIATOR",
  INVITEE = "INVITEE",
}
